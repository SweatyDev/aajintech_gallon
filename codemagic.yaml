workflows:
  ios-please-work:
    name: Put it on iOS NOW!!!

    integrations:
      app_store_connect: Codemagic API

    environment:
      flutter: stable
      xcode: latest

    scripts:
      - name: Get flutter packages, please.
        script: flutter pub get

      - name: Build iOS, pretty please.
        script: flutter build ipa

    artifacts:
      - build/ios/ipa/*.ipa

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
